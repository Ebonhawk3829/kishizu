## What this changes

<!-- What does this do, and why? -->

## Related

<!-- Fixes #123 -->

## Checklist

- [ ] `gofmt -l .` returns nothing
- [ ] `go vet ./...` is clean
- [ ] `go test -race ./...` passes
- [ ] Tests added or updated for the change
- [ ] README updated if behaviour or flags changed
- [ ] No personal data committed (database, config file, watch history)

## If this changes behaviour

<!--
- Does it change what gets downloaded, or when?
- Does it change the database schema? If so, is there a migration step?
- Does it change any default? A default that silently flips is the worst kind
  of change — call it out here.
-->
