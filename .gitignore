# Binaries
/kishizu
*.exe

# Local DB — training state lives here, never commit it
*.db
*.db-shm
*.db-wal

# Scratch
__pycache__/
*.pyc

# Cached scrape attempts (large, regenerable)
testdata/search_cache/

# Design mockups — scratch, not part of the shipped app
concepts/
